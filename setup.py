from setuptools import setup,find_packages
import setuptools
setuptools.setup()

setup(
    name='xuperchain',
    version='0.0.1',
    packages=find_packages()
    # install_requires=[
    #     'grpcio',
    #     'importlib; python_version >= "3.5"',
    # ],
)
